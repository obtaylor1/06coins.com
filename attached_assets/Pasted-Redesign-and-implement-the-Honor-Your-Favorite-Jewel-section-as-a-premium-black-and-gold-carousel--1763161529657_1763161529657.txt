Redesign and implement the “Honor Your Favorite Jewel” section as a premium black-and-gold carousel section where users can browse founder coins, choose quantity, and add to cart.

1. Locate Current Section

Search the codebase for the text: Honor Your Favorite Jewel.

Identify the component or page where this section currently lives (for example HonorJewelSection, Founders, App.jsx, or similar).

We will replace only that section, not the entire page.

2. Create a New Component

Create a new React component, for example:

src/components/JewelCarouselSection.tsx (or .jsx depending on the project)

This component will:

Use React + Tailwind.

Export a default component JewelCarouselSection.

3. Data Model

Inside JewelCarouselSection, define a static array of jewels:

const JEWELS = [
  {
    id: "callis",
    name: "Callis",
    title: "Henry Arthur Callis Coin",
    description: "Commemorative founder coin honoring Henry Arthur Callis.",
    price: 19.06,
    coinImage: "/images/jewels/callis-coin.png",
    portraitImage: "/images/jewels/callis-portrait.png",
  },
  // Chapman, Jones, Kelley, Murray, Ogle, Tandy...
];


Use placeholder paths for now; do not break if the images are missing.

4. Layout & Styling Requirements

Inside JewelCarouselSection, implement this structure using Tailwind:

Section Wrapper

Full-width section with dark background: bg-neutral-950 text-white py-16.

Content centered in a container: max-w-5xl mx-auto px-4.

Hero Text Block

Title: Honor Your Favorite Jewel

Large, elegant font: text-3xl sm:text-4xl font-semibold text-center text-yellow-400.

Subtitle:

Select and purchase individual founder coins to celebrate the legacy of a specific Jewel.

mt-3 text-center text-neutral-300 max-w-2xl mx-auto.

Price line:

$19.06 Each in bold gold.

Subtext: Commemorating the founding year 1906.

Primary CTA button under text:

Label: Browse Founder Jewels

Tailwind: mt-6 inline-flex items-center justify-center rounded-full bg-yellow-500 px-6 py-2 text-sm font-medium text-black hover:bg-yellow-400 transition

This button scrolls down to the carousel using an id anchor or just sits above it visually.

Carousel Container

mt-12 flex flex-col items-center gap-8.

5. Carousel Behavior

Implement a simple custom carousel using useState:

State:

const [activeIndex, setActiveIndex] = useState(0);
const activeJewel = JEWELS[activeIndex];
const [quantity, setQuantity] = useState(1);


Functions:

goNext → increment activeIndex (wrap around).

goPrev → decrement activeIndex (wrap around).

Clamp quantity between 1 and a reasonable max (e.g. 99).

Carousel UI:

A row with:

Left arrow button:
Tailwind: rounded-full border border-yellow-500/40 p-2 hover:bg-yellow-500/10

Centered coin display

Right arrow button

Center Coin Display:

Large circular area:
relative h-44 w-44 sm:h-56 sm:w-56 rounded-full border border-yellow-500/60 bg-gradient-to-b from-yellow-500/10 to-black flex items-center justify-center shadow-xl

Inside, show activeJewel.coinImage (or portrait) as an img with object-cover rounded-full.

Add a soft glow using an extra absolute div behind:
absolute inset-0 rounded-full bg-yellow-500/20 blur-2xl -z-10.

Side Thumbnails:

Under the main coin, show a small horizontal list of all jewels.

Each thumbnail is a small circular image with a gold ring when selected.

Clicking a thumbnail sets activeIndex to that jewel.

Use Tailwind like:
flex gap-3 overflow-x-auto pb-2 and small coins h-14 w-14 rounded-full border.

Ensure the carousel is keyboard accessible (buttons have aria-label).

6. Detail & Add-to-Cart Panel

Under the carousel, add a card:

Wrapper:
w-full max-w-xl rounded-3xl border border-neutral-800 bg-neutral-900/70 backdrop-blur px-6 py-6 flex flex-col gap-4

Content:

Name & Title

activeJewel.name in gold: text-xl font-semibold text-yellow-400.

Subtitle with activeJewel.title or description in text-neutral-300.

Price

$19.06 in larger bold font, aligned left.

Quantity Selector

Label: “Quantity”

Horizontal pill with minus, number, plus:

Parent: inline-flex items-center rounded-full border border-neutral-700 bg-neutral-900

Buttons: small circular, hover:bg-neutral-800

Prevent quantity < 1.

Primary Button

Text: Add Coin to Cart

Full-width gold button: mt-2 w-full rounded-full bg-yellow-500 py-3 text-sm font-semibold text-black hover:bg-yellow-400 transition.

Microcopy

Small text under button:
Ships in 5–7 days • Commemorative packaging included

Tailwind: mt-1 text-xs text-neutral-400 text-center.

7. Cart Integration

If the project already has a cart context or hook (for example useCart or CartContext), integrate like this:

const { addToCart } = useCart(); // adapt to existing API

const handleAddToCart = () => {
  addToCart({
    id: activeJewel.id,
    name: activeJewel.title,
    price: activeJewel.price,
    quantity,
    type: "founder-coin",
  });
};


If no cart exists, implement handleAddToCart as a placeholder that logs to console and is easy to wire up later.

Bind handleAddToCart to the Add Coin to Cart button.

8. Optional Upsell Banner

Below the card, add a small banner:

Background: bg-neutral-900/80 border border-neutral-800 rounded-2xl px-4 py-3 mt-4 flex items-center justify-between gap-3

Text:

Bold: Collect All 7 Founder Coins

Subtext: Special set pricing available.

Right side: a subtle link/button
View Founder Set →

9. Mobile & Responsiveness

Ensure all text and elements respond nicely down to 320px width.

Carousel thumbnails should horizontally scroll on mobile with overflow-x-auto and no ugly scrollbars.

The Add-to-Cart card should remain centered and full width on small screens.

10. Replace Old Section & Export

Replace the old “Honor Your Favorite Jewel” block in the original page with <JewelCarouselSection />.

Ensure there are no TypeScript or ESLint errors.

Run the app to confirm:

Carousel rotates correctly with arrows and thumbnail clicks.

Quantity selector works.

Add to cart handler is wired or clearly stubbed.

Design matches a premium black-and-gold commemorative feel.