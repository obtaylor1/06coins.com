You are a senior full-stack developer and UX expert.
You are working on the existing Admin Dashboard for the Alpha Phi Alpha commemorative coin site.

Your task is to upgrade the admin dashboard’s inventory tracking and analytics using the current dark black + gold theme, without adding a new framework. Reuse the existing tech stack and style approach already in the project.

1. Inspect the current Admin Dashboard

Find the route, template, or file that renders the Admin Dashboard (the one that currently shows: Total Revenue, Total Profit, Coins Sold, Stock Remaining, Inventory Management, and “Customer Orders & Shipping” with “No orders yet”).

Identify:

How data is being passed to this page (server variables, JSON, API call, etc.).

Where inventory and order data structures are defined.

Do not change authentication or routing logic.

2. Add per-product inventory tracking

Assume there are multiple products:

120-Year 4" Anniversary Coin

Complete 7-Jewel Set (3" coins)

The 7 individual 3" Jewel Coins

Update the Inventory Management section to show a table of items, instead of just one “1906 coins” number.

Create a responsive table with columns:

Product (name)

SKU or ID

Current Stock

Reserved (units currently in open/unshipped orders, if available; otherwise 0)

Sold (All-Time)

Reorder Level

Status

Status should be a badge that changes color/text based on stock vs reorder level, e.g.:

In Stock (stock > reorder level)

Low Stock (stock <= reorder level and > 0)

Out of Stock (stock == 0)

Under Current Stock, add quick controls:

Small − and + buttons on each row to decrease/increase by 1.

An “Edit” icon or button that opens a small inline input or modal to set an exact value.

When stock is updated:

Call the existing backend endpoint or create a small new one (e.g. /admin/inventory/update) that updates the database.

Optimistically update the UI.

Show a small toast/alert: “Stock updated for [product name].”

Keep styling consistent: black cards, gold headings, rounded corners, soft shadows.

3. Add low-stock alert summary

Above the inventory table, add a small card titled “Inventory Alerts”.

This card should:

Show the count of products currently at Low Stock or Out of Stock.

List their names in a small bullet list or pill chips.

If no items are low, show a positive message: “All items are sufficiently stocked.”

Use a gold icon (e.g., warning triangle or bell) to draw attention.

4. Add analytics section with charts

Below the KPIs (“Total Revenue”, “Total Profit”, etc.), create a new section: “Sales & Inventory Analytics”.

Within this section, add at least two visualizations using whatever chart solution or custom SVG the project is already set up for (if none, implement simple, lightweight charts with HTML/CSS or a minimal JS chart library already approved in the project).

Chart 1: Sales Over Time

A line chart showing total revenue per day for the last 30 days.

Tooltips on hover: date, revenue, units sold.

X-axis: days; Y-axis: revenue.

Chart 2: Top Selling Products

A horizontal bar chart or ranked list showing units sold per product in the last 30 days.

Each bar/list item should show:

Product name

Units sold

Small subtitle: % of total units

Make this section responsive and visually consistent with the dark admin theme.

5. Add “Inventory Coverage” metrics

In the Inventory table or as a separate column, add “Days of Stock Left” for each product:

Calculate as:
daysOfStock = currentStock / avgDailyUnitsSold

Use a reasonable fallback if there is not enough sales history (e.g., show “—” or “New item”).

Add a small tooltip explaining: “Estimated days before stock reaches zero, based on recent average daily sales.”

If daysOfStock is below a threshold (e.g., < 14 days), automatically mark the item as “Low Stock” in the Status badge.

6. Enhance “Customer Orders & Shipping”

Replace the large empty card that says “No orders yet” with a dynamic orders table.

When there ARE orders:

Show a table with columns:

Order #

Customer Name (or email)

Order Date

Items count (or short summary)

Total Amount

Status (Pending, Shipped, Refunded, etc.)

Allow filtering by status with tabs or a dropdown: All / Pending / Shipped / Refunded.

Allow clicking an order row to view details (navigate to existing order detail page or show a detail modal, depending on current architecture).

When there are NO orders:

Keep a friendly empty state, but smaller: an icon, “No orders yet”, and a line of helper text: “Once customers begin purchasing coins, their orders will appear here for fulfillment and tracking.”

7. UX & Visual Polish

Ensure section headings are clear:

“Inventory Management”

“Inventory Alerts”

“Sales & Inventory Analytics”

“Customer Orders & Shipping”

Maintain the black background + gold accent aesthetic used in the current dashboard.

Use consistent spacing, card radius, and typography.

Ensure the layout works on:

Desktop (two-column layouts where possible: metrics + tables side by side).

Tablet and mobile (stacked cards with full-width tables that scroll horizontally if needed).

8. Code quality and data wiring

Reuse existing data models and routes when possible.

Keep business logic on the server where it already lives; use front-end JS for interaction and small updates only.

Avoid console errors and broken links.

Document any new routes or data structures with short comments in the code.

9. Final requirement

When you are done, the Admin Dashboard should:

Show per-product inventory with quick update controls.

Alert admins to low or out-of-stock items.

Provide visual analytics: sales over time and top-selling items.

Estimate days of stock remaining per product.

Display recent orders with statuses and basic filtering.

Look visually polished and aligned with the current dark gold Alpha aesthetic.

Make all necessary changes now.